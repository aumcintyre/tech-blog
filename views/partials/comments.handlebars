<div>
    {{#each this}}
    <div>
        <div>
            {{comment_message}}
        </div>
        <div>
            {{user.username}}{{format_date created_at}}
        </div>
    </div>
    {{/each}}
</div>